import React from "react";

export default function Footer() {
    return (
        <footer>
            <div>
                <span>{new Date().getFullYear()} Rick & Morty Space </span>
            </div>
            <div>
                <p>Desenvolvido por Guilherme</p>
            </div>
        </footer>
    )
}